CREATE DEFINER=`root`@`localhost` PROCEDURE `loga_usuario`(in senha_entrada varchar(50),in email_entrada varchar(256) )
BEGIN
	SELECT nome FROM usuarios WHERE email_entrada=email AND senha_entrada=senha;
END